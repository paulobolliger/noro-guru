# ==========================================================
# NORO GURU | CONTROL WORKER (Graphile Worker)
# ==========================================================

[build]
builder = "RAILPACK"
buildCommand = "npm run build:worker"
startCommand = "npm run start:worker"
watchPaths = ["packages/control-worker"]

[env]
# Node.js versão fixa
RAILPACK_NODE_VERSION = "22"

# Banco de dados e fila
DATABASE_URL = "postgresql://postgres:[YOUR_PASSWORD]@db.gjffoongddkorxnnzmzc.supabase.co:5432/postgres"
GRAPHILE_CONCURRENCY = "5"

# E-mail / SMTP
SMTP_HOST = "smtp.titan.email"
SMTP_PORT = "587"
SMTP_USER = "noro@noro.guru"
SMTP_PASS = "Gr@bo5207418"
SMTP_FROM = "suporte@noro.guru"

# Outras integrações (opcional)
SENTRY_DSN = ""
