'use server';

const message = 'Ação legada desativada: não há collection Appwrite oficial para este recurso.';

export async function getClienteHistorico(..._args: unknown[]): Promise<any> {
  return [];
}

export async function getClienteTimeline(..._args: unknown[]): Promise<any> {
  return [];
}
