[build]
builder = "RAILPACK"
buildCommand = "npm run build:web"
startCommand = "npm run start:web"
watchPaths = ["apps/control"]

[deploy]
restartPolicyType = "ON_FAILURE"

[env]
RAILPACK_NODE_VERSION = "22"
RAILPACK_NO_SPA = "true"
