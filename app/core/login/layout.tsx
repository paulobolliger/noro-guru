export const metadata = {
  title: 'Login | NORO',
  description: 'Acesso ao núcleo NORO',
}

export default function LoginLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>
}
